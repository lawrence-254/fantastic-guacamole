"use strict";

var semver = require('semver');

var satisfies = function satisfies(range) {
  return semver.satisfies(process.version, range, {
    includePrerelease: true
  });
};

module.exports = {
  satisfies: satisfies
};