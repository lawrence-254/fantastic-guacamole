"use strict";

var tar = require("tar");

var path = require("path");

var tarball = path.resolve(process.argv[2]);
var dirname = path.resolve(process.argv[3]);
tar.extract({
  sync: true,
  file: tarball,
  cwd: dirname
});